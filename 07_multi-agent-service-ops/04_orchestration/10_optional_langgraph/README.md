# 선택: 같은 Orchestration을 LangGraph로 표현하기

먼저 `01~07`의 일반 Python 예제를 이해한 뒤 확인합니다. 새로운 Agent 설계를 배우는 단원이 아니라, 이미 정한 State·Node·Edge·종료를 다른 구현 도구로 옮기는 비교 예제입니다.

```text
START → research → join → itinerary → END
```

# Set this to a public HTTPS URL or a private DNS name when frontend and backend
# are deployed on separate hosts.
BACKEND_URL=http://127.0.0.1:8000

# Frontend-only Compose settings. Use the public HTTPS URL of the backend when
# the frontend and backend run on different servers.
# FRONTEND_PORT=8501
# FRONTEND_IMAGE=ghcr.io/<owner>/simple-multi-llm-frontend:<tag>

[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "aidevs-multi-agent-course"
version = "0.1.0"
description = "07 Multi-Agent Service Ops shared course package"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
include = ["shared*"]


# 05. Swagger Test Result

## 실행 명령

```powershell
uvicorn app.main:app --reload
# 위 명령에서 오류가 나면 아래처럼 실행합니다.
python -m uvicorn app.main:app --reload
```

## 테스트 주소

```text
http://127.0.0.1:8000/docs
```

## 테스트 결과

| API | 결과 | 메모 |
| --- | --- | --- |
| `GET /health` | 성공 / 실패 |  |
|  | 성공 / 실패 |  |
|  | 성공 / 실패 |  |

## 오류가 있었다면

```text
오류 메시지:

해결 방법:
```
